spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: false
      routes: []
    discovery:
      client:
        simple:
          instances: {}

  data:
    redis:
      host: localhost
      port: 6379

eureka:
  client:
    enabled: false

logging:
  level:
    org.springframework: WARN
    org.springframework.cloud.gateway: OFF
    reactor.netty: OFF